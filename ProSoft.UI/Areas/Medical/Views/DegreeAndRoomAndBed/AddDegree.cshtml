@using ProSoft.EF.DTOs.Stocks
@model DegreeRequestDTO
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@{
    ViewData["Title"] = "Add Degree";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@inject IViewLocalizer localizer

<div class="container pt-2">
    <h1 class="mt-4">@localizer["Add Degree"]</h1>
    <form method="post" asp-action="AddDegree">
        <div class="border p-3 mt-4">

            <div asp-validation-summary="ModelOnly"></div>
            <input type="hidden" asp-for="Id" value="@ViewBag.NewCode" />

            <div class="parent d-flex align-items-md-end justify-content-center  my-mobile gap-3 ">
                <div class="mb-3">
                    <label>@localizer["The Code"]</label>
                    <input asp-for="Id" class="form-control" disabled value="@ViewBag.NewCode" />
                    <span asp-validation-for="Id" class="text-danger"></span>
                </div>
                <div class="mb-3 ">
                    <label>@localizer["The Description"]</label>
                    <input asp-for="DegreeName" class="form-control" />
                    <span asp-validation-for="DegreeName" class="text-danger"></span>
                </div>
                
            </div>
            <!------------------------------------------------------------------------------->
            <div class="parent d-flex justify-content-md-center gap-2 ">
                <button type="submit" class="btn btn-primary" style="width:150px">
                    @localizer["Save"]
                </button>
                <a asp-controller="DegreeAndRoomAndBed" asp-action="Index" class="btn btn-secondary" style="width:150px">
                    @localizer["Cancel"]
                </a>
            </div>
        </div>
    </form>
</div>